package com.globant;
import org.testng.annotations.*;

public class RemoveFromCartTest {
    @BeforeClass
    public void setup() {
        // Perform setup actions (e.g., open a browser, navigate to the website, add products to the cart)
    }

    @Test
    public void removeFromCart() {
        // Print actions for removing elements from the cart
        // Example: Remove 3 different elements from the cart and check if the cart is empty.
    }

    @AfterClass
    public void teardown() {
        // Perform cleanup actions (e.g., close the browser)
    }
}
